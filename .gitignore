# Executor execution store — self-ignoring by default.
# The user may delete or narrow this file to commit the execution record;
# see skills/executor/references/safety.md before doing so.
.executor/*

# The tool's thinking store. This repo is the tool, not a consumer of it:
# docs/executor/ only ever fills with residue from local test runs.
docs/executor/

# macOS
.DS_Store
